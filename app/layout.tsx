import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/navbar"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "PrintCraft - Professional Design & Print Studio",
  description:
    "Your trusted partner for professional printing and design services. We specialize in flyers, banners, t-shirt printing, cutting services, branding, and digital printing.",
  keywords: "printing, design, flyers, banners, t-shirts, branding, digital printing, cutting services",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
